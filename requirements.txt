# Core runtime dependencies for SecurityOps Assistant
# Install with:  python -m pip install -r requirements.txt

PySide6>=6.6      # desktop GUI (Qt)
PyYAML>=6.0       # configuration loading
Jinja2>=3.1       # HTML report templating

# PDF report rendering (Linux/Kali). Optional: HTML and Markdown reports work
# without it. security.txt lookup, disclosure emails, and the local LLM client
# use only the Python standard library (urllib, email) — no extra packages.
weasyprint>=60 ; platform_system == "Linux"

# --- Development / test extras (optional) ---
# pytest>=8.0
# pytest-qt>=4.4
# mypy>=1.8
# ruff>=0.3
